# AI 活用プロセスの記録

このポートフォリオサイトおよび掲載作品は、Claude Code / Claude / ChatGPT などの AI ツールとの対話を通じて制作した。
本ドキュメントは、依頼戦略・改善プロセスの要点を記録し、提出物としての透明性を担保することを目的とする。

---

## 共通方針(Vibe Coding のスタンス)

1. **段階的に依頼する** — 「全体像 → 機能仕様 → 実装 → 改善」の順で会話を分け、1回の依頼で全部解決させない
2. **仕様の文書化を先に AI へ依頼** — 実装前に Markdown 等で要件を整理させ、ズレを早期に発見する
3. **AI 出力をそのまま採用しない** — 必ず人間がレビュー・テストし、違和感のあった点はその場で差し戻す
4. **行き詰まったら「別アプローチを N 個提案して」** — 1つの答えに固執せず選択肢を広げる
5. **デザインや既存コードを "契約" として尊重させる** — 「既存の style.css をデザイン契約として扱う」のように明示することで意図しないリファクタを防ぐ

---

## 各成果物の依頼戦略

### 1. テトリスゲーム

| ステップ | AI への依頼の要点 |
|---|---|
| 設計 | 「テトリスの基本ルール(7種のテトリミノ、回転、ライン消去、レベル制)+ 操作仕様を Markdown でまとめて」 |
| 初版実装 | 「上記仕様で HTML/CSS/JavaScript 単一ファイル。配色はサイバーパンク(紫×シアン)」 |
| 改善 | 「T スピン判定を追加」「ハードドロップで落下点プレビュー」「スコアとレベルの UI を右パネルに固定」 |
| 仕上げ | 「キーリピート速度を調整」「ゲームオーバー時のリセット導線を追加」 |

**学び**: ルールが既知のゲームでも、まず仕様文書を作らせる工程を挟むと、後の実装で AI のコンテキストが安定する。

---

### 2. スライド動画 自動編集スキル

| ステップ | AI への依頼の要点 |
|---|---|
| 要件整理 | 「ナレーション音声 + ビジュアルタグ付き台本から、Premiere Pro XML までを自動で組み上げたい。前提・入力・出力を明確化して」 |
| アーキテクチャ | 「Whisper による文字起こし → 台本照合 → スライド/Bロール/テロップのタイミング推定 → XML / SRT 出力 のパイプライン構造を図示して」 |
| スキル化 | 「Claude Code のカスタムスキルとして再利用可能にしたい。`SKILL.md` の triggers / 手順 / 依存パッケージを書いて」 |
| 環境構築自動化 | 「Python パッケージ(openai-whisper / Pillow / PyMuPDF)と ffmpeg の存在チェック → 不足分のインストール手順表示までを起動時に実行」 |

**学び**: 「再利用可能なスキルに仕立てる」という最終ゴールを最初に提示すると、AI は API 設計(コマンド名、引数、出力フォーマット)から逆算してくれる。

---

### 3. このポートフォリオサイト

| ステップ | AI への依頼の要点 |
|---|---|
| レビュー観点の洗い出し | 「ファーストビュー/About/Works/レスポンシブ/OGP/提出条件 などの観点で項目を洗い出して。まだ実装しないで」 |
| 段階レビュー | 「初級 → 中級 → 上級 の三段階で改善案を出して」 |
| 三段運用 | 各段階で「変更ファイル一覧 → ユーザー OK → 実装」の手順を固定 |
| デザイン契約 | 「既存 `style.css` をデザイン契約として扱い、ダーク × サイバーパンクを維持」 |
| アクセシビリティ強化 | 「ハンバーガーメニュー / `prefers-reduced-motion` / `:focus-visible` / skip-link / `<main>` ランドマーク を入れて」 |

**学び**: 「実装の前に変更ファイル一覧を出させて承認制にする」運用は、想定外の変更を防ぎつつ AI の推論を活かせるバランスが良い。

---

## 改善プロセスの工夫

- **承認ゲートを必ず置く**: AI に「実装に入る前に変更内容を一覧で出して」と明示することで、ユーザーが意図と乖離した出力を実装前に止められる
- **スコープを宣言する**: 「今回のスコープは高優先度のみ」のように明示すると、AI は周辺改善まで広げず、ぶれずに作業する
- **既存ファイルを尊重させる**: 「既存の style.css をデザイン契約として扱う」と伝えるだけで、不要なスタイル書き換えを抑制できる
- **検証・記録を依頼に含める**: 実装後に「ブラウザで表示確認 → 結果を一覧で報告」までを依頼に含めるとレビューサイクルが短縮する

---

## 今後の改善案

| 対象 | 改善内容 | 期待効果 |
|---|---|---|
| `nanobanana-hero.png`(1.70 MB) | Squoosh 等で WebP化 + 横1920px / 品質80 → 200〜350 KB | LCP の大幅改善 |
| `nanobanana-about.png`(1.59 MB) | WebP化 + 横800px / 品質80 → 80〜150 KB | About 描画速度向上 |
| `favicon`(現状 1.6MB の流用) | 専用の 32×32 PNG または `.ico` ファイルへ差し替え | タブ表示のために 1.6MB DL を発生させない |
| OGP 画像 | 1200×630 の専用画像を作成、`og:image:width/height/alt` を明示 | SNS シェア時のプレビュー品質向上 |
| Lighthouse 計測 | Performance / Accessibility / Best Practices / SEO の 4 指標を計測 | 改善箇所の数値化と継続改善のサイクル化 |
| 構造化データ(JSON-LD) | `Person` / `WebSite` を追加 | Google 検索結果のリッチ表示 |
| 作品の追加 | 新作の追加と Stats 数値の更新 | 提出物としての厚みを継続的に向上 |
